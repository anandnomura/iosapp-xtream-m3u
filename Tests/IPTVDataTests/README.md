These tests cover package-level logic that can be validated before the Xcode app targets exist:

- M3U parsing
- Xtream endpoint generation

They are not executed in this Windows environment because the Swift toolchain is not installed here.
